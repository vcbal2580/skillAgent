openai>=1.14.0
chromadb>=0.4.22
ddgs>=9.0
fastapi>=0.109.0
uvicorn>=0.27.0
pyyaml>=6.0.1
rich>=13.7.0

# ── Multimodal: Document parsing (optional) ──
pypdf>=4.0.0           # PDF text extraction
python-docx>=1.1.0     # Word .docx extraction
openpyxl>=3.1.0        # Excel .xlsx extraction

# ── Multimodal: Voice input (optional, for /voice CLI and /chat/audio API) ──
# sounddevice>=0.4.6   # microphone recording  (uncomment if needed)
# soundfile>=0.12.1    # local audio file decode for dashscope STT (uncomment if needed)
# dashscope>=1.14.0    # Alibaba DashScope STT  (uncomment if needed)
